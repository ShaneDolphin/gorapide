// Package pattern implements Rapide event-pattern matching and canonical match
// witnesses.
//
// Closed pattern nodes accepted by DeterministicKey form the deterministic
// trusted surface. Predicate callbacks and other opaque host behavior are
// legacy conveniences and are rejected when a pattern is admitted to a
// deterministic architecture, constraint, or map model.
package pattern
